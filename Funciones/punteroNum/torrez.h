#include <stdio.h>
#include <stdlib.h>

float ingresarNumero();
float sumar(float, float);
float restar(float,float);
float multiplicar(float,float);
float dividir(float,float);
float factorial(float,float);
int getInt(int* y, int intentos, char* mes);
